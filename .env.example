# 数据库配置 (用于本地开发)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/lego_price_king?schema=public"

# Docker 部署时 postgres 与 app 共用；不设则默认 postgres。若修改，需在库内执行：
# docker exec -it lego_price_king_db psql -U postgres -c "ALTER USER postgres PASSWORD '你的新密码';"
# POSTGRES_PASSWORD="postgres"

# 远程数据库配置 (用于本地同步脚本)
REMOTE_DATABASE_URL="postgresql://postgres:postgres@your-server-ip:5432/lego_price_king?schema=public"

# 阿里云 OSS 配置
ALIYUN_OSS_REGION="oss-cn-hangzhou"
ALIYUN_OSS_ACCESS_KEY_ID="your-access-key-id"
ALIYUN_OSS_ACCESS_KEY_SECRET="your-access-key-secret"
ALIYUN_OSS_BUCKET="your-bucket-name"
ALIYUN_OSS_ENDPOINT="https://oss-cn-hangzhou.aliyuncs.com"

# 淘宝联盟 API 配置（真实价格与店铺需接入，见 TAOBAO_SETUP_GUIDE.md）
# - TAOBAO_APP_KEY / TAOBAO_APP_SECRET：淘宝开放平台 → 控制台 → 应用详情 / 密钥
# - TAOBAO_ADZONE_ID：淘宝联盟 → 推广管理 → 推广位管理 → 新建推广位 → PID 最后一段数字
TAOBAO_APP_KEY=""
TAOBAO_APP_SECRET=""
TAOBAO_ADZONE_ID=""

# API 密钥：价格刷新接口
# - 留空或保持为占位符 → 不校验，前端「更新价格」可直接用
# - 改为自设密钥 → 需同时设置 NEXT_PUBLIC_API_KEY 为相同值，前端才会带 key 访问
API_SECRET_KEY="your-secret-key-here-change-in-production"

# Next.js 配置
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# 前端 API 密钥：仅当 API_SECRET_KEY 改为自设密钥时，填相同值，否则留空
NEXT_PUBLIC_API_KEY=""
